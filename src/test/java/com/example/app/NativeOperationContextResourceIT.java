package com.example.app;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeOperationContextResourceIT extends OperationContextResourceTest {

    // Execute the same tests but in native mode.
}